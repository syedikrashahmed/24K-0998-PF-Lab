//2. Write a program to check whether a given number is prime or not.
#include <stdio.h>
int main()
{
	int i, num;
	char prime = 'n' 
	printf("enter num: ");
	scanf("%d", &num);
	for (num > 1, num--)
	{
		
	}
}
